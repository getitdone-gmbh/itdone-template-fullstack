version: "1"
services:
  frontend:
    path: ./frontend
    port: 3000
    env:
      VITE_API_URL: ${backend.url}/api

  backend:
    path: ./backend
    port: 8080
    env:
      NODE_ENV: production

addons:
  db:
    type: postgres
    attachTo:
      - backend
